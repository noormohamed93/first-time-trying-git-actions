on:
  push:
    branches:
       - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'printing folder contents'
        run: ls

        -name: 'Testing printing'
         run: echo Ahmed